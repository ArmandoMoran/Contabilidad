spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/contabilidad
    username: contabilidad
    password: contabilidad_dev
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  sat:
    mode: simulated
  pac:
    mode: simulated

logging:
  level:
    com.contabilidad: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
